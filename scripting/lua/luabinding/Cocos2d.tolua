$pfile "cocos2dx.tolua"
$pfile "CocosDenshion.tolua"
$pfile "cocos-ext.tolua"

$[

function tolua.resetcfunction(cls, methodname)
    local cfunction = tolua.getcfunction(cls, methodname)
    if cfunction then
        cls[methodname] = cfunction
    end
end

$]
